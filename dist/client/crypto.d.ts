export declare class Ed25519 {
    private static readonly p;
    private static readonly L;
    private static readonly d;
    private static readonly SQRT_M1;
    private static readonly Gx;
    private static readonly Gy;
    private static readonly G_EXT;
    private static readonly ED25519_OID;
    private static readonly W;
    private static _gTable;
    private static get gTable();
    private static mod;
    private static modInv;
    private static modPow;
    private static extAdd;
    private static extDouble;
    private static extToAffine;
    private static scalarMultG;
    private static scalarMult;
    private static shamirMult;
    private static pointToBytes;
    private static bytesToPoint;
    private static bigIntToBytes;
    private static bytesToBigInt;
    private static sha512;
    private static concat;
    private static clamp;
    private static encodeDerLength;
    private static encodeDerSequence;
    private static encodeDerOctetString;
    private static encodeDerBitString;
    private static parseDerTLV;
    private static parseDerChildren;
    private static unwrapDer;
    private static checkEd25519OID;
    private static base64Encode;
    private static base64Decode;
    private static pemDecode;
    private static pemEncode;
    static getPublicKey(privateKey: Uint8Array): Promise<Uint8Array>;
    static sign(message: Uint8Array, privateKey: Uint8Array): Promise<Uint8Array>;
    static verify(signature: Uint8Array, message: Uint8Array, publicKey: Uint8Array): Promise<boolean>;
    static privateKeyToPem(raw: Uint8Array): string;
    static pemToPrivateKey(pem: string): Uint8Array;
    static publicKeyToPem(raw: Uint8Array): string;
    static pemToPublicKey(pem: string): Uint8Array;
}
